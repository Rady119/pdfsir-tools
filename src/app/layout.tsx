import type { Metadata } from 'next';
import { Inter } from 'next/font/google';
import './globals.css';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'PDFSIR Tools - Professional PDF Processing Platform',
  description: 'Convert, merge, split, compress and edit PDF files online. 50+ professional PDF tools for all your document needs.',
  keywords: 'PDF tools, convert PDF, merge PDF, split PDF, compress PDF, PDF editor',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en" dir="ltr">
      <body className={`${inter.className} antialiased`}>
        <div className="min-h-screen bg-gray-50">
          {children}
        </div>
      </body>
    </html>
  );
}
